DEFAULT_SCENE_INFERENCE_LEFT_PROMPT = """你是一名专业影视场景重投影师和空间连续性校对师。

我会提供同一场景的正面图和反打图。请把这两张输入图当作唯一事实来源，只补全该场景的左侧面视角，不要重新设计场景。

要求：
1. 必须保持与输入正面图、反打图一致的室内/室外属性、空间类型、建筑结构、地面材质、门窗/院墙/屋檐/家具/道具位置关系。
2. 镜头像同一个真实场景中的摄像机移动到左侧拍摄，不是概念设计图，也不是另一个地点。
3. 不得把室外院落生成成室内房间，不得把室内房间生成成室外空间。
4. 不增加与原图冲突的新物体，不删除关键场景元素。
5. 画面中不要有人物、文字、水印、UI。
6. 输出高清 16:9 场景图。

变量：
- 场景风格：{scene_style}
- 输出尺寸：{output_size}
- 模型：{model_name}
- 补充要求：{extra_requirements}
"""

DEFAULT_SCENE_INFERENCE_RIGHT_PROMPT = """你是一名专业影视场景重投影师和空间连续性校对师。

我会提供同一场景的正面图和反打图。请把这两张输入图当作唯一事实来源，只补全该场景的右侧面视角，不要重新设计场景。

要求：
1. 必须保持与输入正面图、反打图一致的室内/室外属性、空间类型、建筑结构、地面材质、门窗/院墙/屋檐/家具/道具位置关系。
2. 镜头像同一个真实场景中的摄像机移动到右侧拍摄，不是概念设计图，也不是另一个地点。
3. 不得把室外院落生成成室内房间，不得把室内房间生成成室外空间。
4. 不增加与原图冲突的新物体，不删除关键场景元素。
5. 画面中不要有人物、文字、水印、UI。
6. 输出高清 16:9 场景图。

变量：
- 场景风格：{scene_style}
- 输出尺寸：{output_size}
- 模型：{model_name}
- 补充要求：{extra_requirements}
"""

DEFAULT_SCENE_INFERENCE_TOP_PROMPT = """你是一名专业影视场景重投影师和空间连续性校对师。

我会提供同一场景的正面图和反打图。请把这两张输入图当作唯一事实来源，只补全该场景的鸟瞰俯视图，不要重新设计场景。

要求：
1. 镜头为俯瞰视角，合理展示输入图对应的同一个场景整体空间布局。
2. 必须保持与输入正面图、反打图一致的室内/室外属性、空间类型、建筑结构、地面材质、门窗/院墙/屋檐/家具/道具位置关系。
3. 不得把室外院落生成成室内房间，不得把室内房间生成成室外空间。
4. 不增加与原图冲突的新物体，不删除关键场景元素。
5. 画面中不要有人物、文字、水印、UI。
6. 输出高清 16:9 场景图。

变量：
- 场景风格：{scene_style}
- 输出尺寸：{output_size}
- 模型：{model_name}
- 补充要求：{extra_requirements}
"""

DEFAULT_SCENE_INFERENCE_PANORAMA_PROMPT = """你是一名专业影视场景重投影师和全景空间连续性校对师。

我会提供同一场景的多视角图片：正面图、反打图、左侧面图、右侧面图、俯瞰图。请把这些输入图当作唯一事实来源，生成同一空间的 equirectangular 2:1 全景图。

要求：
1. 输出完整的 360 度全景图。
2. 图像比例必须为 2:1，适合 equirectangular 全景查看器旋转查看。
3. 左右边缘无缝衔接。
4. 必须保持与输入图一致的室内/室外属性、空间类型、建筑结构、场景风格、材质、光线和色调。
5. 场景空间关系合理统一，不得生成另一个地点、另一个房间或另一个院落。
6. 不要人物、文字、水印、UI。
7. 默认目标尺寸为 3072 × 1536 或同等 2:1 高清规格。

变量：
- 场景风格：{scene_style}
- 输出尺寸：{output_size}
- 模型：{model_name}
- 补充要求：{extra_requirements}
"""
