DEFAULT_SCENE_INFERENCE_LEFT_PROMPT = """你是一名专业影视场景设计师和空间推理设计师。

我会提供同一场景的正面图和反打图，请基于这两张图推理出该场景的左侧面视角。

要求：
1. 保持与原图一致的画风、材质、光线、色调和时代背景。
2. 保持场景空间结构一致，像同一个真实场景从左侧摄像机拍摄。
3. 不增加与原图冲突的新物体。
4. 不删除关键场景元素。
5. 画面中不要有人物、文字、水印、UI。
6. 输出高清 16:9 场景图。

变量：
- 场景风格：{scene_style}
- 输出尺寸：{output_size}
- 模型：{model_name}
- 补充要求：{extra_requirements}
"""

DEFAULT_SCENE_INFERENCE_RIGHT_PROMPT = """你是一名专业影视场景设计师和空间推理设计师。

我会提供同一场景的正面图和反打图，请基于这两张图推理出该场景的右侧面视角。

要求：
1. 保持与原图一致的画风、材质、光线、色调和时代背景。
2. 保持场景空间结构一致，像同一个真实场景从右侧摄像机拍摄。
3. 不增加与原图冲突的新物体。
4. 不删除关键场景元素。
5. 画面中不要有人物、文字、水印、UI。
6. 输出高清 16:9 场景图。

变量：
- 场景风格：{scene_style}
- 输出尺寸：{output_size}
- 模型：{model_name}
- 补充要求：{extra_requirements}
"""

DEFAULT_SCENE_INFERENCE_TOP_PROMPT = """你是一名专业影视场景设计师和空间推理设计师。

我会提供同一场景的正面图和反打图，请基于这两张图推理出该场景的鸟瞰俯视图。

要求：
1. 镜头为俯瞰视角，合理展示场景整体空间布局。
2. 保持与原图一致的画风、材质、光线、色调和时代背景。
3. 不增加与原图冲突的新物体。
4. 不删除关键场景元素。
5. 画面中不要有人物、文字、水印、UI。
6. 输出高清 16:9 场景图。

变量：
- 场景风格：{scene_style}
- 输出尺寸：{output_size}
- 模型：{model_name}
- 补充要求：{extra_requirements}
"""

DEFAULT_SCENE_INFERENCE_PANORAMA_PROMPT = """你是一名专业影视场景设计师和全景图生成设计师。

我会提供同一场景的多视角图片：正面图、反打图、左侧面图、右侧面图、俯瞰图。请基于这些图片生成一张 equirectangular 2:1 全景图。

要求：
1. 输出完整的 360 度全景图。
2. 图像比例必须为 2:1，适合 equirectangular 全景查看器旋转查看。
3. 左右边缘无缝衔接。
4. 保持与输入图一致的场景风格、材质、光线和色调。
5. 场景空间关系合理统一。
6. 不要人物、文字、水印、UI。
7. 默认目标尺寸为 3072 × 1536 或同等 2:1 高清规格。

变量：
- 场景风格：{scene_style}
- 输出尺寸：{output_size}
- 模型：{model_name}
- 补充要求：{extra_requirements}
"""
